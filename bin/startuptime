#!/usr/bin/env sh
GIT_ROOT=$(git rev-parse --show-toplevel)

exec nix develop "$GIT_ROOT" --command \
  nvim --startuptime "$GIT_ROOT/assets/startuptime.txt" \
       "$GIT_ROOT/nvim/init.lua" \
       +q
